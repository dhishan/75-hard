# GCS backend configuration for the dev workspace
# Used with: terraform -chdir=terraform/main init -backend-config=../workspaces/dev/backend.conf
bucket = "YOUR_PROJECT_ID-tfstate"
prefix = "75-hard/dev"
